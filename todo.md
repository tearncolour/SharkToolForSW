# 基本要求
！！！请使用最新的SolidWorks API
1.编写主要新功能请创建新文件，防止单个文件过大
2.需要任何额外的需要我补充的资料/网址请联系我
3.每个步骤之间请用中文说明（这很重要）
4.请用中文注释
5.执行任何指令时应该考虑是否会终止上一个指令
6.先全局分析再动手 - 完整读取所有相关组件的结构和样式
使用对比法 - 当有工作的参考实现时（如FileExplorer），先完整对比差异
系统排查 - 从最外层容器逐层向内检查，而不是只盯着目标元素
验证假设 - 每次修改前明确说明假设和预期效果
7.不用考虑跨系统，只考虑Windows系统
# 前置约束
1. 业务场景：SolidWorks插件多级缓存（内存缓存+磁盘缓存），用于优化大型装配体加载速度，核心对象为轻化几何数据（顶点、面、配合约束）与SpeedPak配置。
2. 技术栈：C# + SolidWorks API，.NET Framework 4.7.2，使用ConcurrentDictionary实现线程安全内存缓存，使用BinaryFormatter进行二进制序列化存储磁盘缓存文件。
3. 架构要求：采用分层设计，缓存逻辑与业务逻辑解耦；实现接口抽象，支持内存/磁盘缓存策略灵活切换；添加版本校验与异常降级机制。
4. 性能要求：内存缓存使用线程安全容器；磁盘缓存读写异步执行，避免阻塞SolidWorks主线程；缓存键设计为 零件路径+修改时间戳+版本号 ，确保数据一致性。
5. 规范要求：代码需包含完整注释（类注释、方法注释、关键变量注释）；遵循SOLID原则；添加日志记录（缓存命中/失效/写入）；所有SolidWorks API调用需加try-catch捕获COM异常
---
## 待办事项
优化插件安全性

核心原则：主线程仅执行轻量API交互，耗时操作异步隔离，COM对象严格生命周期管理，操作节流防抖，功能按需降级
 
## 通用前置约束（所有功能均需遵守）
 
1. 线程隔离铁律
- SolidWorks API 对象（ IModelDoc2 / IAssemblyDoc / ISketchManager 等）仅允许在主线程创建和访问，后台线程严禁直接调用API。
- 耗时操作（如遍历装配体结构、生成草图几何、序列化数据）必须封装到  Task.Run  中，主线程只负责传递非API数据（如零件路径、几何参数）。
2. COM对象零泄漏管控
- 遍历集合必用for循环：禁用 foreach 遍历 IFeature / IComponent2 等COM集合，避免隐式创建未释放的中间对象。
- try-finally强制释放：所有COM对象使用后必须在 finally 块中通过 Marshal.ReleaseComObject 释放，即使对象为 null 也要校验。
- 禁止缓存COM对象：不将 IComponent2 / IFace2 等对象存入全局变量或缓存，仅缓存其属性数据（如ID、名称、坐标）。
3. 操作节流与防抖
- 高频事件加防抖：对 ModelRebuildDone / SelectionChange 等高频触发事件，设置200-500ms防抖窗口，避免短时间内重复执行逻辑。
- 批量操作合并执行：创建草图时，合并多个草图实体的创建指令（如一次性添加所有直线/圆），减少API调用次数；遍历装配体时，先过滤掉压缩状态的零件，减少遍历数量。
## 全局稳定性保障
 
1. 异常快速降级：所有API调用和耗时操作都包裹 try-catch ，捕获 COMException / NullReferenceException 后，立即终止当前操作，恢复SolidWorks默认状态，不影响主程序运行。
2. 资源占用限制：内存缓存设置最大容量，超过阈值时采用LRU策略淘汰数据；后台线程数量限制为CPU核心数的1/2，避免线程过多抢占资源。
3. 插件退出清理：在插件卸载事件中，清空所有缓存、释放剩余COM对象、恢复SolidWorks的原始偏好设置（如自动重建、显示模式），防止进程残留导致卡顿。